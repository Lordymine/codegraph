# reference clone of the upstream project — not part of our repo
/_upstream/

# binaries
/codegraph
/codegraph.exe
*.exe

# local graph stores / scratch
*.db
*.db-wal
*.db-shm
/tmp/

# quality-harness run artifacts (repo-specific paths; regenerate with `quality gen`)
/quality-run/

# multi-repo eval: clones + per-repo run artifacts (protocol + repos.json stay tracked)
/eval/checkouts/
/eval/runs/

# session scratch: one-off retrofit measurement scripts + captured agent output.
# The official methodology lives in quality-workflow.js + internal/quality + docs/QUALITY.md.
/eval/_*.json
/eval/measure-vta.js
/eval/splice-items.js
/eval/intra-repo-truth.js
